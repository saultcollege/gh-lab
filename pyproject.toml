[project]
name = "gh-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rodney Martin", email = "rodney.martin@saultcollege.ca" }
]
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]

[project.scripts]
gh-lab = "gh_lab:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
